apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: worker
  namespace: airflow
spec:
  selector:
    matchLabels:
      app: worker
  serviceName: worker
  replicas: 2
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: watchdg/airflow:1
          imagePullPolicy: Always
          args:
            - worker
          ports:
            - containerPort: 8793
          envFrom:
            - secretRef:
                name: airflow